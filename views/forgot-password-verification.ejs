<%- include('./header.ejs') %>
<form action="/submit-forgot-password-verification" method="post">
    <section class="login-sec">
        <div class="container">
            <div class="row align-items-center">
                <div class="col-md-5 col-lg-5 m-auto">
                    <div class="login-form-box">
                        <h2>Verify</h2>
                        <% if(err_msg!=undefined){ %>
                            <span class="error_validate flash_msg" style="color:red" >
                              <%= err_msg %>
                            </span>
                          <% }%>
                          <% if(success_msg!=undefined){ %>
                            <span class="error_validate flash_msg" style="color:#71D840" >
                              <%= success_msg %>
                            </span>
                          <% }%>
                        <div class="form-group mb-0">
                            <label>Enter Your OTP</label>
                            <input
                                type="password"
                                name="otp"
                                class="form-control"
                                placeholder="******"
                                autocomplete="off"
                                id="otp-field"
                            />
                            <span toggle="#otp-field" class="fa fa-fw fa-eye field-icon toggle-password"></span>
                        </div>
                        <p class="text-right forgot-link">
                            <a href="/forgot-password-verification-email">Resend OTP</a>
                        </p>
                        <div class="mb-3">
                            <input class="common-btn" type="submit" value="Submit" />
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>
</form>

<script>
  $(".toggle-password").click(function() {
    $(this).toggleClass("fa-eye fa-eye-slash");
    var input = $($(this).attr("toggle"));
    if (input.attr("type") == "password") {
      input.attr("type", "text");
    } else {
      input.attr("type", "password");
    }
  });
</script>

<%- include('./footer.ejs') %>
